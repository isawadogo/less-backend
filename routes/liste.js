const express = require('express');
const router = express.Router();

/* Route : GET - List Budget by cathegories - /liste/budget/:month
IN : current month
Returns :
  OK = { result: true, monthBudget: the month budget }
  KO = { result: false, error: error_message }

Description : This route retrieves the spending by categori for the current month
*/

// 1 - faire une route qui récupère les listes par mois

// 2 -  faire une route qui récupère les montants par catégories

modules.exports = router;